SET(CMAKE_RUNTIME_OUTPUT_DIRECTORY "${SF1RLOGSERVER_ROOT}/testbin")
INCLUDE_DIRECTORIES(
  ${CMAKE_SOURCE_DIR}/thirdparty/
  ${CMAKE_CURRENT_SOURCE_DIR}

  ${izenelib_INCLUDE_DIRS}
  )

ADD_SUBDIRECTORY(mlist)
